<Window x:Class="Crud_Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Crud_Demo"
        mc:Ignorable="d"
        Title="AdminPanel" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>
        <DockPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="1" Grid.RowSpan="1" LastChildFill="True" >
            <TextBox x:Name="ID" DockPanel.Dock="Top" Margin="0,0,0,2" />
        </DockPanel>
        
        <DockPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="1" Grid.RowSpan="1" LastChildFill="True">
            <TextBox x:Name="Name" DockPanel.Dock="Top" Margin="0,0,0,2"/>
        </DockPanel>
        
        <DockPanel Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="1" Grid.RowSpan="1" LastChildFill="True">
            <TextBox x:Name="Username" DockPanel.Dock="Top" Margin="0,0,0,2"/>
        </DockPanel>
        
        <DockPanel Grid.Row="1" Grid.Column="3" Grid.ColumnSpan="1" Grid.RowSpan="1" LastChildFill="True">
            <TextBox x:Name="Address" DockPanel.Dock="Top" Margin="0,0,0,2"/>
        </DockPanel>
        
        <DockPanel Grid.Row="1" Grid.Column="4" Grid.ColumnSpan="1" Grid.RowSpan="1" LastChildFill="True">
            <TextBox x:Name="Age" DockPanel.Dock="Top" Margin="0,0,0,2"/>
        </DockPanel>
        
        <DataGrid ItemsSource="{Binding Employees}" x:Name="usersGrid" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="5" Grid.RowSpan="2" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" SelectionChanged="usersGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="*"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
                <DataGridTextColumn Header="Age" Binding="{Binding Age}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Create"  Click="Create" Grid.Column="0" Grid.Row="4"/>
        <Button Content="Read" Click="Read" Grid.Column="1" Grid.Row="4"/>
        <Button Content="Update"  Click="Update" Grid.Column="2" Grid.Row="4"/>
        <Button Content="Delete" Click="Delete" Grid.Column="3" Grid.Row="4"/>
        <Button Content="Clear" Click="ClearGrid" Grid.Column="4" Grid.Row="4"/>
    </Grid>
</Window>
